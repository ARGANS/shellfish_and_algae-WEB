version: '3.9'

services:
  acdashboard:
    container_name: ac-dashboard
    stdin_open: true
    build:
      context: ../dashboard/
      dockerfile: ./prod.Dockerfile
    ports:
      - "$DASHBOARD_EXT_PORT:80"
    networks:
      - ac_net

networks:
  ac_net:
